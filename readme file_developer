npx prisma generate   
npx prisma migrate dev --name init 
use the se two command before runnnig the app




Your current flow is:

ACTIVE Journey
      ↓
Check lastHeartbeatAt
      ↓
More than 2 minutes old?
      ↓
YES
      ↓
deviceStatus = UNREACHABLE
      ↓
Check if notification already exists
      ↓
No existing notification?
      ↓
createAndDeliverNotification()
      ↓
Email trusted contact